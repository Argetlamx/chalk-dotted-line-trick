# 本项目的持久上下文

开始本项目任务时，先阅读根目录 [MEMORY.md](MEMORY.md)，然后核对当前文件、Git状态和用户最新指示。

- 使用中文协作。项目为IYPT 2027第12题，两人、三个月、500元，必须保留预实验和容错安排。
- 用户已授权本仓库常规访问、修改及任务范围内同步；保留队友内容和历史，不强制覆盖，不自动提交无关本地资料。
- Git访问失败先检查本机代理，具体方法见MEMORY.md，不将Git直连失败等同于用户网络不可用。
- 公式排版已由用户修复：保留原生Markdown数学块，不重新加入导致此项目显示异常的LaTeX `\tag` 命令，不继续已放弃的SVG转换方案。
- 区分文献结论、理论预测和实测结果，不编造数据。
- 重要项目事实和同步方法发生变化时更新MEMORY.md；不将密码、Token或私钥写入记忆。
