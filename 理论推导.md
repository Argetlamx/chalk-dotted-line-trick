# 粉笔虚线：竖直平移—转动双自由度模型

更新：2026-09-20。依据用户本次建模；配套：[装置设计](实验装置设计.md)、[实验方案](实验方案.md)、[文献综述](文献研究综述.md)。旧竞争模型保存在[历史理论](docs/历史方案/理论推导.md)，不与本篇符号混用。以下为待检验模型，不是实测结论。

## 1. 当前研究问题与几何约定

0. 构建机械模拟手握粉笔的装置，粉笔可拆换，不用热熔胶固定。
1. 在固定结构参数下，预载 $N_0$、黑板速度 $v$、初始角度 $\theta_0$ 如何共同决定是否发生黏滑？
2. 初始角度到起跳角度的变化 $\Delta\theta$ 如何依赖这些参量？
3. 检验点距 $\lambda$ 是否在明确的参数区间内正比于 $v$。

装置采用**水平放置的黑板、水平移动黑板、竖直移动夹持轴心**。这是对用户“竖直恢复力”的实施约定；若黑板竖立，竖直不再是法向，下面重力与几何方程必须重写。

使用实验室系：$X$ 沿黑板运动方向，$z$ 竖直向上，板面 $z=0$；$y$ 沿转轴。轴心 $P=(0,h)$，粉笔尖 $Q=(-L\cos\theta,h-L\sin\theta)$。$\theta$ 为从笔尖指向轴心的向量与板面 $+X$ 的夹角。图示工况 $0<\theta<90^\circ$，黑板向右为 $v>0$；反向实验用负 $v$，不要无声更换角度定义。

$L$ 是**转轴到尖端**距离；$L_c$ 是**夹口到尖端**露出长度，二者不相等。主装置不再有柔性梁长度 $L_b$。粉笔与夹头在一轮中视作刚性组合；磨损改变 $L$ 后重新标定。

## 2. 恢复力的零点与预载

用户的 $F=-kx$、$M=-\gamma\theta$ 应用于相对弹簧参考位置的变形。为了不把板面夹角和弹簧扭转量混为一谈，写成：

$$
F_z=F_b-k(h-h_r)-c_h\dot h,\qquad
M_\theta=-\gamma(\theta-\theta_r)-c_\theta\dot\theta.
$$

$F_z$ 为弹簧对滑台组件的向上合力，不含重力或接触力；$F_b,h_r$ 描述竖直预紧，$\theta_r$ 是弹簧零力矩参考角。设 $\varphi=\theta-\theta_r$，便得到用户的 $M=-\gamma\varphi$。$k$ 单位 N/m，$\gamma$ 单位 N·m/rad，计算必须用弧度。阻尼项是待标定的现实修正。

**初始板面角 $\theta_0$ 一般不等于 $\theta_r$。** 带预载接触时，需要非零弹簧力矩平衡接触反力。把二者强行相等会丢掉静态平衡条件。仅当定义的是相对预载平衡态的“增量力矩”，才可写 $\delta M=-\gamma\,\delta\theta$，同时仍保留平衡力矩。

## 3. 惯性与动力学方程

设转动组件（粉笔、笔套、转臂等）质量为 $m$；其他只随滑台平移的质量为 $m_s$；$m_t=m_s+m$。本版简化假设转动组件质心位于 $P$ 到 $Q$ 的轴线上，距 $P$ 为 $a$，关于转轴的转动惯量为 $J$。若实物质心偏轴，应配平或用实际质心二维坐标重建动能；不要直接套均匀粉笔的 $J=mL^2/3$。

动能与重力势能：

$$
K=\frac12m_t\dot h^2-ma\cos\theta\,\dot h\dot\theta
+\frac12J\dot\theta^2,\qquad
U_g=m_tgh-mga\sin\theta.
$$

令尖端所受切向力为 $F_t$（向右正），法向力为 $N$（向上正）。拉格朗日方程给出：

$$
m_t\ddot h-ma\cos\theta\,\ddot\theta
+ma\sin\theta\,\dot\theta^2=F_z-m_tg+N,
$$

$$
J\ddot\theta-ma\cos\theta\,\ddot h
=M_\theta+mga\cos\theta
+L\sin\theta\,F_t-L\cos\theta\,N.
$$

如果转臂/笔套造成质心偏轴，不必强行加配重：设质心相对P的竖直坐标为已测几何函数 $z_c(\theta)$，则更一般的方程为：

$$
m_t\ddot h+mz_c'(\theta)\ddot\theta
+mz_c''(\theta)\dot\theta^2=F_z-m_tg+N,
$$

$$
J\ddot\theta+mz_c'(\theta)\ddot h
=M_\theta-mgz_c'(\theta)+L\sin\theta F_t-L\cos\theta N.
$$

质心固定在转子上，可用两个方向悬挂/平衡找位置，再依实际转子角变换求 $z_c$。当 $z_c=-a\sin\theta$ 时还原前面的轴线模型。以下静态式采用轴线近似，偏轴时对应将重力矩换为 $-mgz_c'(\theta_0)$。

每项分别具有力或力矩的量纲。质量矩阵应满足 $m_tJ-(ma\cos\theta)^2>0$；实际夹头和滑台质量不可忽略。

初始静态接触条件：

$$
h_0=L\sin\theta_0,\qquad
N_0=m_tg-F_z(h_0,0),
$$

$$
0=-\gamma(\theta_0-\theta_r)+mga\cos\theta_0
+L\sin\theta_0F_{t0}-L\cos\theta_0N_0.
$$

静止时可能有残余切向力 $F_{t0}$，不能默认一定为零。调预紧、调零力矩角后应反复微动板面、重新落笔，并检查重复性；秤测 $N_0$，侧视测 $\theta_0$。有两项独立调整仍不代表一次旋钮操作就能独立设定二者。

## 4. 黏着、滑动、离板分别判定

几何间隙及相对滑动速度为：

$$
d=h-L\sin\theta,\qquad
u=L\sin\theta\,\dot\theta-v.
$$

刚性单边接触满足 $d\ge0,\ N\ge0,\ dN=0$。持续接触时 $d=0$；黏着时还需 $u=0$、$|F_t|\le\mu_sN$，其 $F_t$ 是由约束求出的反力，不能在黏着阶段恒设为 $\mu_sN$。

滑动的最低阶模型：

$$
F_t=-\mu_k(|u|)N\,\operatorname{sgn}(u).
$$

常数 $\mu_k$ 与速度相关摩擦均可作竞争假设；$\mu_s,\mu_k$ 未标定前没有唯一的数值相界。对移动板，正确的耗散检查是 $F_tu\le0$；$F_t\dot X_Q$ 本身可以为正，能量由驱动黑板输入。

在接触且黏着的区段：

$$
\dot\theta=\frac{v}{L\sin\theta},\qquad
\dot h=v\cot\theta.
$$

所以竖直弹簧与转动弹簧的储能都会变化。可对这两个约束求导，再代入动力学求所需 $N,F_t$，直到摩擦约束失效或接触反力降至零。它们不适用于飞行或一般滑动段。

离板条件是约束求解所需的法向力降至零，且之后趋向分离；单有 $N=0$ 的瞬时点不足以保证一段飞行。离板后取 $N=F_t=0$，继续积分两自由度方程。再次接触需要碰撞/接触柔度模型；恢复系数或法向刚度、阻尼须独立估计及做敏感性检验。尚未构建数值求解器，也未得到起跳角解析定律。

## 5. 起跳角与角度变化的操作定义

静止、已调好预载时测得 $\theta_0$；首次可辨离板的时刻为 $t_{\rm lift}$：

$$
\theta_{\rm lift}=\theta(t_{\rm lift}),\qquad
\Delta\theta_{\rm first}=\theta_{\rm lift}-\theta_0.
$$

保留有符号差，同时可报告绝对值；不预设一定增大。稳态每周期则另报：

$$
\Delta\theta_{\rm cycle,i}
=\theta(t_{\rm lift,i})-\theta(t_{\rm contact,i}).
$$

首次启动与稳态周期不是同一个观测量。实验同时记录第一次运动开始到起跳的角度变化，以及后续周期的分布。若缓慢加速，第一次起跳受启动曲线影响，不应仅归因于最终匀速 $v$；首次试验需要统一并保存启动曲线。

侧视间隙超过三倍综合定位不确定度且持续至少两帧，才能归为可辨离板。阈值通过时刻是有延迟的估计：优先给最后接触帧到首次确认帧的角度区间，必要时用轨迹插值并报告方法。没起跳、起跳看不清都记 NA 并说明原因，不能填 $\Delta\theta=0$。

## 6. 点距正比于速度的适用条件

板面材料坐标为 $s=X_Q-vt$。两个同相位事件的有向位移满足：

$$
\Delta s=X_Q(t+T)-X_Q(t)-vT.
$$

只有稳定周期且每周期对应一个可辨点时，$X_Q(t+T)=X_Q(t)$，点距的大小才为：

$$
\lambda=|v|T=\frac{|v|}{f}.
$$

因此 $\lambda\propto|v|$ 还要求 **$T$ 在研究速度区间近似不变**。非周期段、每周期多点、点中心与接触事件不一一对应时，需要先修正事件定义。检验时固定 $N_0,\theta_0,k,\gamma,L$、质量及表面状态，并独立由侧视录像计时 $T$；不能用 $\lambda/v$ 构造周期再称为独立验证。

接触约束 $h=L\sin\theta$ 使竖直弹簧转化为转动方向的弹性贡献。其切线项含 $kL^2\cos^2\theta$，但还包括预载、重力与几何项；不能把 $\sqrt{J/\gamma}$ 当作接触运动唯一时间尺度。

## 7. 参数识别与结论边界

相界应写成条件关系：

$$
P(\text{黏滑}\mid N_0,v,\theta_0;
k,\gamma,L,m_t,J,\text{阻尼、摩擦、表面状态}).
$$

主试验固定分号后的装置条件，测三参量的联合影响；边界可能是概率过渡而非一条锐利曲线。仅靠 $N_0,v,\theta_0$ 三个数不能脱离夹具和材料预测普适边界。

装置验收优先测 $F_z(h)$、$M_\theta(\theta)$ 的加载/卸载曲线及交叉耦合，再测自由振动、摩擦区间。建模第一步比较连续接触、黏滑、离板类别；后续仿真才讨论定量相界。文献中的Painlevé机制是参考，不是本装置已成立的结论。
