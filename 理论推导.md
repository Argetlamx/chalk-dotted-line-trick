# 粉笔虚线：从可测量量到竞争模型的理论推导

版本：2026-09-09。配套：[文献综述](文献研究综述.md)、[实验方案](实验方案.md)。

本文给出可用于实验检验的推导，不声称已获得本项目参数或数值仿真结果。一般摩擦与接触框架参考综述R2、R3；下面的符号、简化模型及检验安排由本项目重新组织。不能把一个模型的成立范围扩展到全部虚线现象。

## 1. 坐标、长度与观测量

以黑板为参考系，x沿平均书写方向，z垂直板面向外。水平板实验中重力沿−z；若改为竖直板，必须重新分解重力。

| 符号 | 定义 | 单位/测法 |
|---|---|---|
| X(t)、V | 夹具基座相对板面的位移、平均速度 | m、m/s，视频跟踪 |
| x(t)、z(t) | 粉笔接触端的位置，z=0为几何板面 | m，侧视标定 |
| θ | 从板面+x方向指向“笔尖到夹持点”的有向角，0°至180° | 侧视图；θ与180°−θ区分推/拖 |
| Lc | 粉笔从夹持口到笔尖的伸出长度 | m |
| Lb | 柔性支撑梁的自由长度 | m，与Lc分开记录 |
| N(t)、N0 | 瞬时法向反力、静态接触预载 | N；电子秤只能测后者 |
| k、c、m | 指定运动方向的等效刚度、阻尼、有效质量 | N/m、N·s/m、kg |
| λi | 相邻痕迹中心在板面上的距离 | m；不等于空白长度 |
| wi、gi | 点/短划宽度、其后空白长度 | m；阈值决定其边界 |
| T、f、D | 循环周期、1/T、实际接触时间占比 | s、Hz、无量纲 |

二维俯视照片中的痕迹宽度应注明是沿书写方向还是横向。后文w表示沿书写方向的宽度。

## 2. 点距与周期的联系：先得到无需指定摩擦律的关系

令基座匀速X=Vt，笔尖相对基座位移u=x−X。若进入周期状态，间隔T后u(t+T)=u(t)。选取相同相位的两个落点：

$$
x(t+T)-x(t)=VT+u(t+T)-u(t)=VT.
$$

因而每周期对应一个点时：

$$
\boxed{\lambda=VT=V/f}. \tag{1}
$$

这一式子是**周期运动的运动学关系**，不是“速度越大点距必然线性增大”的证明；只有T近似不变才有线性关系。若每个周期产生两个可分辨点，按点数得到的频率将为循环频率的两倍。

速度不恒定时，应使用相同相位间的基座位移积分并保留相对位移差：

$$
\lambda_i=\int_{t_i}^{t_{i+1}}\dot X(t)\,dt+u(t_{i+1})-u(t_i). \tag{2}
$$

接触占空比D也不能直接写成w/λ。只有笔尖在接触期近似以V移动、单次接触对应单一痕迹且显影响应近似均匀时，才有w≈VDT。黏着、回滑、磨损阈值会破坏它。

## 3. 模型A：只有切向自由度的黏滑

### 3.1 方程和稳态失稳

假设笔尖一直接触，法向力恒定N0。夹持等效为移动弹簧与阻尼：

$$
m\ddot x=k(Vt-x)+c(V-\dot x)-F_f(\dot x). \tag{3}
$$

在正向滑动时令Ff=N0μ(v)。稳态解为x0=Vt−N0μ(V)/k。令x=x0+η，一阶展开：

$$
m\ddot\eta+[c+N_0\mu'(V)]\dot\eta+k\eta=0. \tag{4}
$$

因此对这个光滑、一维、恒法向载荷模型：

$$
c+N_0\mu'(V)<0 \tag{5}
$$

意味着线性负阻尼失稳。μ随速度下降只是可能克服机械阻尼的来源；μ′<0本身不够。式(5)也不是含离板系统的必要条件。

稳态附近若阻尼较小，振动尺度为ω0=√(k/m)，但真正的有限振幅周期还取决于摩擦非线性和接触切换。

### 3.2 理想库仑黏滑的低速点距尺度

用静摩擦上限μsN0和动摩擦μkN0，定义ΔF=(μs−μk)N0>0。暂取c=0。笔尖黏着时，基座继续移动，弹簧力以kV的速率增加。达到静摩擦上限后滑动。

在滑动阶段短到可忽略基座位移的极限，设滑动起点为ξ=0，初速度0：

$$
m\ddot\xi+k\xi=\Delta F,
\qquad
\xi(t)=\frac{\Delta F}{k}[1-\cos(\omega_0t)]. \tag{6}
$$

首次回到零速度时ts≈π/ω0，滑动距离约为2ΔF/k。随后弹簧需重新增加约2ΔF的力才能再次滑动：

$$
T_{stick}\simeq\frac{2\Delta F}{kV},\qquad
\boxed{\lambda\simeq\frac{2(\mu_s-\mu_k)N_0}{k}}
\quad(V\to0). \tag{7}
$$

条件为ε=kV/(ΔFω0)≪1，并且滑动结束后可重新黏着。式(7)是该理想模型的低速首项；有限速度修正为O(V/ω0)，不能仅把πV/ω0相加就宣称得到精确修正。阻尼和速度/状态相关摩擦也会改变系数。

**可检验预测**：若加载时间主导，λ对V依赖较弱，f随V增加；提高k倾向于缩短λ。N0效应只在其他参数不变时才有式(7)所示趋势。

**关键缺口**：这个模型只有切向运动，不能生成z>0。即使产生黏滑，也还需材料转移模型才能预测明暗点迹。看到干净空白而没有分辨出接触状态，不能自动认可或排除模型A。

## 4. 模型B：柔性支撑决定的振动时间尺度

### 4.1 自由振动的可测量参数

单一模态近似：

$$
m\ddot q+c\dot q+kq=0,
\qquad \zeta=\frac{c}{2\sqrt{km}},
\qquad \omega_d=\sqrt{\frac{k}{m}}\sqrt{1-\zeta^2}. \tag{8}
$$

当ζ<1时，相邻同向峰值的对数衰减率δlog满足：

$$
\delta_{log}=\frac1n\ln\frac{A_j}{A_{j+n}},\qquad
\zeta=\frac{\delta_{log}}{\sqrt{4\pi^2+\delta_{log}^2}}. \tag{9}
$$

由独立静态k和自由振动ωd估算

$$
m_{eff}=\frac{k(1-\zeta^2)}{\omega_d^2}. \tag{10}
$$

仅当静态加载方向与被跟踪模态一致，且确由单模态主导时才这样解释；多模态情况下报告各峰频率，不硬凑一个m。

### 4.2 梁长度如何影响周期

均匀细长悬臂梁、小挠度、端部横向力F的静态解给出：

$$
\delta_b=\frac{F L_b^3}{3EI},\qquad k_b=\frac{3EI}{L_b^3}. \tag{11}
$$

矩形截面I=bh³/12，h是弯曲方向厚度；圆截面I=πd⁴/64。材料未知时先测k，不从网上给橡胶或粉笔挑一个E。

若端部附加质量mt占主导：

$$
T_0\approx2\pi\sqrt{\frac{m_tL_b^3}{3EI}}\propto L_b^{3/2}. \tag{12}
$$

若梁自身分布质量主导，Euler–Bernoulli方程为：

$$
EI\,\frac{\partial^4 y}{\partial s^4}+
\rho A\,\frac{\partial^2y}{\partial t^2}=0,
\quad
\omega_1=\frac{\beta_1^2}{L_b^2}\sqrt{\frac{EI}{\rho A}},
\quad\beta_1\approx1.8751, \tag{13}
$$

其中ρ为体积密度，ρA才是线密度。边界是根部位移/斜率为零、自由端弯矩/剪力为零。此时T0∝Lb²。一个可用的静态形状Rayleigh近似为meff≈mt+(33/140)mb，但端部质量、胶带和非均匀梁会改变模态；实验自由振动结果优先。

这些长度律是**柔性支撑梁**的预测，不等于“粉笔伸出长度的普适幂律”。若粉笔本体也弯曲或握持点转动，须加入相应柔度；只有一维受力串联近似成立时，才可用1/keff≈1/k支撑+1/k夹持+1/k粉笔。

### 4.3 从结构周期到点距

若接触循环周期T≈αT0，则：

$$
\boxed{\lambda\approx\alpha VT_0}. \tag{14}
$$

α由预载、角度、接触事件和阻尼决定，不预设为1或3/4。对于近似固定结构和相同接触模式，可检验λ/V是否近似恒定；跨刚度组则比较λ/(VT0)。若曲线不塌缩，不能强行认为结构频率控制一切。

## 5. 模型C：允许离板的最小二维模型

模型B只给时间尺度。要判断能否离板以及何时返回，需要至少法向与切向两个自由度。

### 5.1 结构与力

用接触端代表质量m，基座参考位置rb=(Vt,−h)。h是装置几何偏置，不直接等于N0/k。笔尖位置r=(x,z)，d=r−rb。柔性支撑力为−Kd−C·ḋ。

令支撑主要轴向为e∥=(cosθ,sinθ)，横向e⊥=(−sinθ,cosθ)：

$$
K=k_\parallel e_\parallel e_\parallel^T+
k_\perp e_\perp e_\perp^T,
\qquad K_{xz}=(k_\parallel-k_\perp)\sin\theta\cos\theta. \tag{15}
$$

C同样可由两主方向阻尼组成。真实夹具不沿粉笔方向时，应另测支撑主轴角φ，以φ替代式(15)中的θ；不要把几何笔角与刚度方向强行等同。

水平板上运动方程为：

$$
m\ddot x=-(Kd)_x-(C\dot d)_x+F_t,
$$
$$
m\ddot z=-(Kd)_z-(C\dot d)_z-mg+N. \tag{16}
$$

当Kxz≠0时，切向位移可调制法向恢复力。改变推/拖方向也可能改变耦合符号。这给出了检验方向效应的原因，而非只靠“摩擦大”解释。

该模型把转动凝聚进等效柔度，适用于小幅运动。若录像显示显著刚体转动，应升级为q=(xc,zc,θ)并保留转动惯量，不能继续把式(16)当精确方程。

### 5.2 刚性板的接触模式与切换条件

非黏附接触满足：

$$
z\ge0,\qquad N\ge0,\qquad zN=0. \tag{17}
$$

接触滑动时Ft=−μkN sgn(ẋ)；黏着时ẋ=0且|Ft|≤μsN。所有速度均相对于板面。

| 模式 | 如何求解 | 离开该模式的条件 |
|---|---|---|
| 黏着 | z=0，ẋ=ż=0；由式(16)求约束力Ft、N | 所需切向力超过μsN，或维持接触需N<0 |
| 接触滑动 | z=ż=z̈=0，解N和切向方程 | ẋ到零后检查能否黏着；或N降到零且无接触自由加速度向外 |
| 飞行 | N=Ft=0，积分两方向运动 | z下降到零且ż<0时发生碰撞 |

从接触态计算维持z̈=0所需反力：

$$
N_{req}=(Kd)_z+(C\dot d)_z+mg. \tag{18}
$$

若Nreq<0，板面必须“拉住粉笔”才能维持接触，这与非黏附假设冲突，因此应脱离。**在持续接触计算中出现负N，不应继续代入摩擦式；但也不能仅凭负N就宣布出现Painlevé悖论。**

### 5.3 为什么不直接套一个反弹系数

斜接触的切向/转动冲量与法向碰撞耦合，简单地只翻转法向速度可能给出错误能量。此处建议用有限接触刚度解析碰撞过程，而不把复杂刚体碰撞规律作为三个月项目主线。[接触正则化背景](https://arxiv.org/html/1601.03545v1#S2.SS4)

可用的非黏附软接触模型是：

$$
N=\begin{cases}
\max(0,-k_nz-c_n\dot z),&z<0,\\
0,&z\ge0.
\end{cases} \tag{19}
$$

这里z<0表示数值模型的微小压入量，不是真实宏观穿透。碰撞损失由cn控制，不再同时加一个恢复系数以重复计算耗散。

切向可先用平滑滑动近似

$$
F_t=-N\left[\mu_k+(\mu_s-\mu_k)
e^{-(|v|/v_s)^2}\right]\tanh(v/v_\epsilon). \tag{20}
$$

vs为摩擦下降速度尺度，vε为平滑尺度，须区分二者。式(20)在v=0时Ft=0，**不能保持有限静摩擦，不能用来定量验证黏着驻留时间**。若实测有清晰黏着段，须采用式(17)的静摩擦约束或带接触切向弹性并限幅的摩擦模型。常数μk版本也要作为对照，检查是否仅靠结构耦合就能解释观测。

### 5.4 能量来源

在滑动中Ft·ẋ≤0，摩擦消耗笔尖机械能；阻尼同样耗能。振动维持所需能量来自移动基座做功。一个稳定周期中，结构动能与弹性能净变化为零，基座输入功与摩擦、阻尼及碰撞耗散相平衡。摩擦的作用是调制能量传递与稳定性，并非凭空供能。

## 6. 痕迹形成需要单独的观测模型

设单位时间转移的粉笔量为Q(N,|v|,表面状态)，沿板面坐标s的累积沉积可表示为：

$$
M(s)=\int Q(t)\,G[s-x(t)]\,\mathbf1_{contact}(t)\,dt. \tag{21}
$$

G为有限接触斑的空间核。照片亮度还受照明、相机曝光、粉末散射和饱和影响，不能直接把像素灰度当压力。式(21)仅定义观测关系，尚不是经过标定的磨损定律。

即使没有离板，Q变化也可能形成明暗相间；即使离板，笔尖宽度或飞散粉末也可能使空白不完全黑。因此模型比较以运动学为主，痕迹为补充。

## 7. 如何用有限数据区分模型

| 假设 | 可检验量 | 支持它的结果 | 会削弱它的结果 |
|---|---|---|---|
| A：加载时间主导 | λ(V)、f(V)、可分辨黏着时间 | 低速λ近常数，f约随V增加 | 大部分周期为法向飞行且T跟随自由模态 |
| B：结构时间主导 | λ/V与独立T0 | T近常数，改变刚度后T同步变化 | 固定结构下T系统性随1/V变化 |
| C：耦合离板 | z(t)、θ方向、接触占比 | 空白与离板同步，推/拖明显不对称 | 高分辨观测显示持续接触，仍形成强周期点迹 |
| 混合机制 | T=t加载+t振动 | λ≈a+bV，且相位分解支持两个时间尺度 | 只有拟合线，找不到对应相位 |

可先比较λ=a、λ=bV、λ=a+bV；参数a为长度、b为时间。不能仅凭非零截距断言存在黏滑，也不能仅凭高R²断言机制正确。至少以一组未用于拟合的速度和一天独立重复做预测检验。

建议无量纲量：Λ=λ/(VT0)、加载比Π=kVT0/[(μs−μk)N0]、阻尼比ζ，以及主刚度比k∥/k⊥。若μs≈μk或差值误差过大，不计算不稳定的Π，直接报告原始可测量量。

## 8. 参数辨识和数值实施边界

1. 独立测量k、T0、θ、Lc、N0和V；没有独立数据的参数给区间，不同时让十个参数自由拟合点距。
2. 先用式(1)、(7)、(14)做数量级和趋势检验。只有确认离板且简单尺度失败，才投入二维积分。
3. 数值初始条件用静态预载平衡：初始x位移也受Kxz影响，不设所有位移、力同时为零。
4. 式(19)的接触时间尺度约τn=√(m/kn)。从Δt≤min(T0/200,τn/30)起步，逐次减半检验λ、D和峰值压入量；刚性较强时用隐式求解器。这个步长规则是起点，不是收敛保证。
5. 检查Ft·v≤0、N≥0、最大压入量远小于可观测位移尺度；检查基座做功与耗散平衡。对kn、cn、vε做敏感性扫描，若点距强依赖数值参数则不能称为可靠预测。
6. 保存初始条件、舍弃暂态的规则和最终轨迹；正反向速度扫描检查多稳态。周期不稳时报告分布，不拿一次跳跃代替极限环。

## 9. 理论与实际结论的分界

目前已经得到的是：三个可检验模型、条件明确的尺度律、接触切换方程和识别路径。尚未得到的是：你们材料的摩擦参数、稳定虚线区间、真实飞行周期或任何数值拟合结果。

最终报告应按“测到什么 → 排除了什么 → 哪个模型在何范围成立”组织。若只验证出一个窄区间，同样能构成严谨结果；不需要在500元项目中证明一般多体Painlevé理论。
