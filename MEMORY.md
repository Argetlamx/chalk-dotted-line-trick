# 项目记忆

更新日期：2026-09-20。此文件是仓库中的持久项目上下文，由 AGENTS.md 指引后续任务读取；不等同于应用内置自动记忆数据库。执行前仍须核对实际文件与远端状态。

## 仓库访问

- 仓库：https://github.com/Argetlamx/chalk-dotted-line-trick
- 远端 `origin`：`https://github.com/Argetlamx/chalk-dotted-line-trick.git`。
- 主分支 `main`，跟踪 `origin/main`。
- 当前用户电脑的目录：`D:\grade1-semester1\experiments\chalk dotted line`；终端为 Windows PowerShell。
- 用户已通过 Git Credential Manager 登录，实际 fetch/push 已成功。使用本机凭据管理器，不在聊天或文件中保存密码、Token、私钥。
- 用户授权助手直接访问、修改并按任务要求同步此仓库，供两人共享研究资料。正常同步要保留队友内容与提交历史。
- 本仓库提交署名为 `Chalk Research Team <chalk-research@example.invalid>`。这是用户允许任取的提交署名，邮箱为占位地址，不是联系邮箱，也不是登录凭据。

## 代理与执行环境

浏览器能访问 GitHub，不代表 Git 直连也可用。2026-09-09曾出现 Git 到 github.com:443 超时/连接重置；检查发现 Windows 使用本地代理 `127.0.0.1:7897`，Git 未使用。让 Git 使用同一代理后，读取和推送成功。

本机已保存仓库级配置：

```powershell
git config --local http.proxy http://127.0.0.1:7897
```

它不会随 clone 同步。只在这台电脑且代理运行时有效；队友必须按自己的网络配置，不能照搬端口。再次失败应核实代理服务、Windows代理与Git配置，不将Git连接失败直接描述为用户网络不可用。

临时连接验证：

```powershell
git -c http.proxy=http://127.0.0.1:7897 ls-remote --symref origin
```

`.git` 曾由沙箱账户创建，用户账户操作时可能报 `dubious ownership`。核实是这个项目后，对当前命令指定精确目录即可：

```powershell
git -c safe.directory='D:/grade1-semester1/experiments/chalk dotted line' status
```

必要时对其余Git命令加同样前缀，不使用 `safe.directory=*`。普通执行环境还曾报 `helper_unknown_error: setup refresh had errors`，这属于执行环境问题，与GitHub认证或代理故障不同；按环境权限流程执行已授权命令。

## 同步步骤

1. `git status --short --branch`，检查用户未提交及未跟踪文件，不自动删除、覆盖或全部暂存。
2. `git fetch origin`，检查远端提交；无本地冲突时 `git merge --ff-only origin/main`。工作区干净时也可直接 `git pull --ff-only`。
3. 如发生分叉，先检查双方提交再合并，不使用强制推送或丢弃用户内容的reset。
4. 完成任务后按具体文件名 `git add -- 文件名`，检查 `git diff --cached` 和 `git diff --cached --check`。
5. `git commit -m '说明本次改动'`，再 `git push origin main`。
6. 比对 `git rev-parse HEAD` 与 `git ls-remote origin refs/heads/main`；确认相等后才报告已同步。推送失败时明确区分本地完成与远端完成。

正式文档名为 `文献研究综述.md`、`理论推导.md`、`实验方案.md`。远端初始资料和本地历史已合并；不要恢复旧的数字前缀文件名造成重复和断链。

## 公式排版：问题已由用户解决

- 2026-09-12用户确认问题已解决，描述为 `\tab` 冲突；核对远端提交 `4bcca6c`，实际改动为移除21处 `\tag{…}` 编号命令，保留原生 `$$` 公式。
- 保留该修复，不重新加入 `\tag`，不继续之前提出的SVG替换路线。
- 如果后续需要编号，优先放在公式块外的普通文字中。正文仍有“式(1)”等历史引用，未来编辑涉及这些引用时再核对编号对应关系，本次未改公式。
- 先前仅安装了本地 `tools/` 下的 mathjax-full 依赖；SVG生成器、SVG图片、公式转换和记忆文件写入在当时没有成功执行。不能把它们记成已完成。

## 项目重要内容

- 研究对象：IYPT 2027 第12题 Dotted line trick，连续拖动粉笔产生周期性点迹的控制参数。
- 团队2人，研究时间3个月，总经费人民币500元。初始13周计划为2026-09-09至2026-12-09，后续日期变动需明确更新。
- 主预算400元，备用金100元；必须保留前两周预实验和最后两周补测缓冲。前两周累计支出上限150元。
- 2026-09-20用户确定新主模型：粉笔夹持轴心水平固定、竖直可动，竖直回复F=-kx和转动回复M=-γφ；水平黑板匀速v。θ0为初始板面角，φ=θ−θr，θr为零力矩角，不能混同。
- 新主装置：竖直微型导轨滑台＋水平单轴轴承夹头＋上下竖直拉簧＋随滑台运动的左右对拉转臂弹簧。用户要求容易网购、无3D打印、粉笔可更换，禁止热熔胶粘粉笔；夹头使用笔套/螺钉V槽夹。旧单柔性梁仅作历史/预实验对照。
- 研究0装置，1 N0/v/θ0联合黏滑边界，2 Δθ首次及周期起跳角变化，3检验λ∝v；最后一项要求独立验证T是否恒定，不能预设成立。
- 实验装置设计.md及figures/机械握持器设计.png、.svg保存详细布局、尺寸建议、装配、标定、采购上限与来源；尚未实物验证。低成本导轨寄生摩擦是关键风险，须验收；备选双簧片导向有水平寄生位移，不能声称完全等价。
- 独立校准v、静态N0、θ0、θr、k、γ、自由振动和交叉耦合；换角后匹配N0。L是转轴至笔尖、Lc是夹口至笔尖；新主装置不使用Lb。首次起跳与稳态周期Δθ分开，启动曲线必须记录。
- 三条竞争路线：切向黏滑与加载时间、结构振动时间尺度、法向/切向耦合的离板与碰撞。周期状态下有 λ=VT=V/f，但不代表T恒定。
- 不能凭虚线照片断言黏滑、离板或Painlevé悖论。优先侧视轨迹、接触检测下限与同条痕迹互证。
- 2023 IPT有近似同题、官方五页报告与其他公开研究。综述区分期刊研究、赛事报告、摘要证据及全文未核查的情况。
- 目前确认完成的是研究准备文档，没有本项目已经开展实验的证据。文献结论、理论假设与实测结果必须区分，不虚构实验数据。
- 新矩阵替代旧176/221条：A三参量108条+B边界30条+C比例24条+D对照12条=174核心；可选γ变化18条；预实验/标定/插入基准另计。三个月截止仍为12月09日，9月20日之后排程见新实验方案，不声称已有实验完成。
- 每条实验保存唯一run_id，关联原始录像、照片、校准和分析；保留失败工况，做跨天复现与两人交叉复核。

## 文件与本地资料

- 四份主文档为文献、理论、实验方案、实验装置设计；README为导航，data/templates为记录模板。原理论/实验移至docs/历史方案并标为停止执行，保留历史研究内容。
- `research_sources/`为忽略的文献下载和阅读截图缓存，引用原始网页链接，不能让队友依赖本地缓存。
- 2026-09-12本地还有未跟踪的 `essay/`（课程模板/例文）、`references/`（参考论文）、`tools/`（上次安装的依赖）。当时仅提交记忆文件；这些目录继续不随文档任务自动上传，也不提交node_modules。
- 原始录像与 `data/raw/` 默认不进Git；推荐数据目录、记录字段、预算和周计划详见实验方案。
- 更新实际实验进度、预算、装置或访问方法时同步更新本记忆。不得记录凭据或把本机代理当作所有队员通用设置。

## 2026-09-20目录变更

原D:\grade1目录已不存在，实际项目在D:\grade1-semester1\experiments\chalk dotted line；应用工作区路径仍可能过时。发现无效目录先核实路径，不当作网络错误。读写/同步使用实际路径并遵守权限流程。未跟踪essay/、references/、tools/及graph1.ggb、graph2.ggb、graph3.ggb继续保留，不纳入本次提交。
